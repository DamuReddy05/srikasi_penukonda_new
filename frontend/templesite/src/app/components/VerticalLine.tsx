import { Box } from "@mui/material";

const VerticalLine = () => {
  return (
    <Box
      sx={{
        width: "1px",
        height: "150px",
        backgroundColor: "rgba(255, 255, 255, 0.3)",
      }}
    />
  );
};

export default VerticalLine;
